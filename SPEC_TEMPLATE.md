# Спецификация фичи: [НАЗВАНИЕ]

## Описание
[Что делает, для кого, зачем — 2-3 предложения]

## User Stories
- Как [роль], я хочу [действие], чтобы [результат]
- Как [роль], я хочу [действие], чтобы [результат]
- Как [роль], я хочу [действие], чтобы [результат]

## Модель данных
[Новые таблицы или изменения существующих. Формат Prisma schema.]
```prisma
model NewModel {
  id        String   @id @default(uuid())
  // поля с типами
  createdAt DateTime @default(now())
}
```

## API
[Эндпоинты: метод + путь + тело запроса + ответ + коды ошибок]
- `POST /api/feature` — `{field: type}` → 201 `{result}` | 400 | 401
- `GET /api/feature/:id` → 200 `{result}` | 404

## Экраны и компоненты
[Какие страницы/компоненты, что отображают, состояния]
- **FeaturePage** — описание, состояния: loading, error, empty, success
- **FeatureModal** — описание

## Бизнес-логика
[Правила валидации, ограничения, формулы, условия]

## Крайние случаи
[Что может пойти не так и ожидаемое поведение]
- Сценарий → Ожидаемое поведение
- Сценарий → Ожидаемое поведение

## Приоритет
[high / medium / low]

## Зависимости
[От каких модулей зависит, что должно быть готово до начала]
